import styled from "styled-components";

const ProductsWrapper = styled.div`
  .products {
    .img {
      width: 80%;
      height: 400px;
      object-fit: cover;
    }
  }
`;

export default ProductsWrapper;
